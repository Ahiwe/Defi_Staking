function randomNumber() {
    return Math.random;
}

let randomNumber = () => 
    Math.random
//  bracket are only necessary because we have multiple 
//  arguement or entities to represent else we can do without  it 
 
